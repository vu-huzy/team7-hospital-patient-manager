# Database Configuration
# Copy this file to .env and update with your MySQL credentials

DB_HOST=localhost
DB_PORT=3306
DB_USER=root
DB_PASSWORD=your_mysql_password_here
DB_NAME=hospital_manager

# Flask Configuration (for web app)
FLASK_ENV=development
SECRET_KEY=change-this-to-random-secret-key-in-production

# Instructions:
# 1. Copy this file: cp .env.example .env
# 2. Edit .env file with your MySQL password
# 3. Never commit .env file to Git (it's in .gitignore)
